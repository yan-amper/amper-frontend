export const RESPONSE_CODE_ADMIN_PANEL = {
  200: "Успешно сохранен",
  500: "Не корректный запрос, попробуйте по другому",
};

export const RESPONSE_CODE_PRODUCTR = {
  500: "Упс, Ваш запрос не можем обработать, попробуйте что нибудь другое",
  503: "Упс, в данный момент сервер не работет, попробуйте позже",
};

export const CAPACITY_RANGES = [
  "35-42",
  "45-50",
  "55-65",
  "70-85",
  "90-110",
  "130-230",
];

/**
 * Контакты магазина — один источник правды.
 * Адрес и режим работы раньше были продублированы в футере и в секции
 * «Наш магазин», а телефон в секции адреса вообще был захардкожен
 * мимо переменной окружения.
 *
 * Строки намеренно с обычными пробелами: за переносы отвечает CSS
 * (text-wrap: pretty в футере), а не невидимые U+00A0 в исходнике.
 */
export const SHOP_ADDRESS = "г. Таганрог, Мариупольское шоссе, д. 1";
/**
 * Короткая форма для первого экрана: город там уже назван в заголовке,
 * а полный адрес в одну строку с режимом работы не помещался и переносился.
 */
export const SHOP_STREET = "Мариупольское шоссе, 1";
export const SHOP_HOURS = "Каждый день 8:30 – 18:30";
/**
 * Короткая ссылка на карточку организации в Яндекс.Картах, а не поиск
 * по адресу: по адресу открывалась просто точка на карте, а здесь —
 * сам «Ампер» с телефоном, часами, отзывами и кнопкой маршрута.
 */
export const SHOP_MAP_URL = "https://yandex.ru/maps/-/CHTdi0yK";

/**
 * Сколько товаров показывать в секциях-витринах на главной.
 * Четыре — это ровно один ряд на десктопе и 2×2 на планшете, без
 * «осиротевшей» карточки в последнем ряду. Полный список — по кнопке
 * под секцией.
 */
export const HOME_SECTION_LIMIT = 4;

/**
 * Сколько грузить в мобильную ленту. Там карточки листаются, и лишние
 * не ломают раскладку, а дают, что листать. На десктопе всё, что дальше
 * четвёртой, прячется — см. Rail в секции популярных.
 */
export const HOME_SLIDER_LIMIT = 6;
